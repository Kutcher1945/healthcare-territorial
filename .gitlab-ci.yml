stages:
  - containerize
  - deploy-k8s

variables:
  DOCKER_BUILD_ARGS: >
    --build-arg CACHEBUST=$CI_PIPELINE_ID

include:
  - project: 'opendata/devops/templates'
    file: 'build/containerize.yaml'
  - project: 'opendata/devops/templates'
    file: 'deploy/k8s-deploy.yaml'